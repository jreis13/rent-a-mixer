<%= simple_form_for @service do |f| %>
  <%= f.input :name, placeholder: " Your service" %>
  <%= f.input :price, placeholder: "What's the price?" %>
  <%= f.input :description, placeholder: "Say something..." %>
  <%= f.submit "Add", class: "btn btn-login" %>
<% end %>
